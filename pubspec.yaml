name: fire_manager
description: Firebase app to manage the list of company, rate, tag e.t.c.
version: 1.0.0+1
environment:
  sdk: ">=2.7.0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter
  get_it: ^4.0.4
  injectable: ^1.0.4
  auto_route: ^0.6.7
  stacked: ^1.7.6
  stacked_services: ^0.5.2
dev_dependencies:
  lint: ^1.2.0
  auto_route_generator: ^0.6.8
  injectable_generator: ^1.0.4

flutter:
  uses-material-design: true
